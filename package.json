{
  "name": "luxury-wedding-invitation",
  "version": "1.0.0",
  "private": true,
  "description": "Premium luxury digital wedding invitation website with React + Express + MongoDB",
  "scripts": {
    "install:all": "npm --prefix frontend install && npm --prefix backend install",
    "dev:frontend": "npm --prefix frontend run dev",
    "dev:backend": "npm --prefix backend run dev",
    "dev": "concurrently -n FRONTEND,BACKEND -c yellow,cyan \"npm run dev:frontend\" \"npm run dev:backend\"",
    "build:frontend": "npm --prefix frontend run build",
    "start:backend": "npm --prefix backend run start",
    "seed": "npm --prefix backend run seed"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
